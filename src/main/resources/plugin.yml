name: ${project.name}
description: ${project.description}
version: ${project.version}
prefix: ${project.name}
api-version: ${paper-api.version}
main: ${project.groupId}.Mod
authors: [ nothub ]
depend: [ ProtocolLib ]

commands:
  ghostkick:
    description: drop packets to a specified user until rejoin
  ghostban:
    description: drop packets to a specified user until unban
  ghostunban:
    description: unban specified user
  ghostlist:
    description: list active ghost kicks and bans
